# 服务端口（容器内默认 3001，部署平台需映射该端口）
SERVER_PORT=3001

# 数据库地址。本地位开发默认 file:./dev.db
# 生产环境（Docker）默认 file:/data/prod.db，/data 需挂载持久卷
DATABASE_URL=file:/data/prod.db

# 运行环境：development | production
NODE_ENV=production
